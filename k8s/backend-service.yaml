apiVersion: v1
kind: Service
metadata:
  name: navlambda-backend
  namespace: navlambda-studio
  labels:
    app: navlambda-backend
    component: backend
spec:
  selector:
    app: navlambda-backend
  ports:
  - port: 8080
    targetPort: 8080
    name: http
  type: ClusterIP